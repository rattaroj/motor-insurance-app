# backend
bin/
obj/
# frontend
node_modules/
.next/
.env.local
*.tsbuildinfo
# misc
*.user
CLAUDE.MD
.claude*
# local Liquibase config (machine-specific connection / auth)
liquibase.properties
# local JDBC drivers (downloaded for Liquibase, not source)
db/drivers/
# editor / IDE workspace settings
.vscode/
# TestSprite-generated tests, fixtures and logs
testsprite_tests/
# uploaded runtime files (driver ID-card images — PII, not source)
backend/src/MotorInsurance.Api/wwwroot/uploads/
# local fixtures + scratch files
fixtures/
geo.tmp.json
